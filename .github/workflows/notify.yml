name: Notify ntfy on push
on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Read version from package.json
        id: pkg
        run: echo "VERSION=$(jq -r .version package.json)" >> $GITHUB_ENV

      - name: Send notification to ntfy
        env:
          AUTH: ${{ secrets.NTFY_AUTH }}
        run: |
          curl -u "$AUTH" \
            -H "Title: Mise à jour Villa Marguerite" \
            -H "Priority: high" \
            -H "Tags: house" \
            -d "
            Yo 👋
            🚀 Nouveauté : ${{ github.event.head_commit.message }}
            📦 Version actuelle : $VERSION
            👉 Consultez-le ici : https://villa-marguerite.vercel.app/
            " \
            https://ntfy.sh/villa-marguerite
